#Preprocessing : crop every photos into small "windows"
from customised_functions import *

img_folder = "path to the folder of photos to be cropped"
destination_folder = "path to the folder where you want to store the cropped images"

grid_cropping(img_folder,destination)